fastapi
uvicorn[standard]
pydantic
python-dotenv
ollama
requests
# palimpsest is not on PyPI -- clone https://github.com/ScottColeSW/Palimpsest.git
# as a sibling of this repo and install it into this venv with:
#   pip install -e ../../Palimpsest
